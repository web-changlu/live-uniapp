<template>
	<view class="page-container">
		<!-- 内容区域 -->
		<view class="content">
			<!-- 页面标题 -->
			<view class="title-container">
				<text class="page-title">开播设置</text>
				<text class="page-subtitle">设置您的直播信息</text>
			</view>

			<!-- 表单区域 -->
			<view class="form-container">
				<view class="form-item">
					<text class="form-label">直播标题</text>
					<view class="input-wrapper">
						<input class="form-input" v-model="liveTitle" placeholder="请输入开播标题" placeholder-style="color: #a0a0a0;" />
					</view>
				</view>

				<view class="form-item">
					<text class="form-label">直播描述</text>
					<view class="textarea-wrapper">
						<textarea class="form-textarea" v-model="liveDescription" placeholder="请输入开播描述" placeholder-style="color: #a0a0a0;"></textarea>
					</view>
				</view>

				<view class="form-item">
					<text class="form-label">直播封面</text>
					<view class="cover-section">
						<view class="cover-button" @click="selectCover">
							<text class="cover-button-icon">📷</text>
							<text class="cover-button-text">选择封面</text>
						</view>
						<view v-if="coverImage" class="cover-preview">
							<image :src="coverImage" class="cover-image" mode="aspectFill" />
							<view class="cover-remove" @click="removeCover">
								<text class="remove-icon">✕</text>
							</view>
						</view>
					</view>
				</view>
			</view>

			<!-- 开始直播按钮 -->
			<view class="action-area">
				<view class="start-button" @click="startLive">
					<text class="start-button-text">开始直播</text>
				</view>
			</view>
		</view>
	</view>
</template>

<script setup lang="ts">
import { ref, onMounted } from 'vue';
import permision from "@/TrtcCloud/permission.js";
import genTestUserSig from '@/debug/GenerateTestUserSig';
import { useLiveStore, useUserStore, useTrtcStore } from '@/store'

// 使用store
const liveStore = useLiveStore();
const userStore = useUserStore();
const trtcStore = useTrtcStore();

const liveTitle = ref('我的精彩直播');
const liveDescription = ref('欢迎来到我的直播间，一起互动吧！');
const coverImage = ref('');
const userId = ref('user_' + Math.floor(Math.random() * 100000).toString());

const selectCover = () => {
	uni.chooseImage({
		count: 1,
		sizeType: ['original', 'compressed'],
		sourceType: ['album', 'camera'],
		success: (res) => {
			coverImage.value = res.tempFilePaths[0];
		},
	});
};

const removeCover = () => {
	coverImage.value = '';
};

const startLive = async () => {
	if (!liveTitle.value || !liveDescription.value) {
		uni.showToast({
			title: '请填写完整信息',
			icon: 'none',
		});
		return;
	}

	// 显示加载中
	uni.showLoading({
		title: '准备直播中...'
	});

	try {
		
		// 开始直播
		const liveResult = await liveStore.setLiveData({
			title: liveTitle.value,
			description: liveDescription.value,
			coverImage: coverImage.value,
			userId: userStore.userId,
			userName: userStore.userName,
			userAvatar: userStore.userAvatar,
		});

		if (!liveResult.success) {
			throw new Error(liveResult.error || '开始直播失败');
		}

		const roomId = liveResult.liveId;

		// 隐藏加载中
		uni.hideLoading();

		// 显示成功提示
		uni.showToast({
			title: '直播开始',
			icon: 'success',
		});

		// 跳转到直播详情页
		uni.redirectTo({
			url: `/pages/live/detail/index?roomId=${roomId}`
		});
	} catch (error) {
		// 隐藏加载中
		uni.hideLoading();

		// 显示错误提示
		uni.showToast({
			title: error.message || '开始直播失败',
			icon: 'none',
		});

		// 重置直播状态
		liveStore.resetLiveState();
	}
};

const checkPermission = () => {
	if (uni.getSystemInfoSync().platform === 'android') {
		permision.requestAndroidPermission('android.permission.RECORD_AUDIO');
		permision.requestAndroidPermission('android.permission.CAMERA');
	}
}

const getUserSig = () => {
	// 生成 userSig
	const { sdkAppId: testAppId, userSig: testUserSign } = genTestUserSig(userId.value);
	// 登录用户
	userStore.login({
		userId: userId.value,
		userName: `用户${userId.value.substring(5)}`,
		userSig: testUserSign,
		sdkAppId: testAppId,
		permissions: {
			canLive: true,
			canComment: true,
			canSendGift: true
		}
	});

	if (!testAppId || !testUserSign) {
		uni.showToast({
			title: '请填写 sdkAppId、userSig',
			icon: 'none'
		});
	}
}

onMounted(() => {
	checkPermission();
	getUserSig();
})
</script>

<style>
/* 页面容器 */
.page-container {
	flex: 1;
	width: 750rpx;
	background-color: #f6f8fb;
}
/* 内容区域 */
.content {
	flex: 1;
	padding: 40rpx 30rpx;
	flex-direction: column;
}

/* 标题容器 */
.title-container {
	flex-direction: column;
	align-items: center;
	margin-bottom: 10rpx;
}

/* 页面标题 */
.page-title {
	font-size: 24rpx;
	font-weight: bold;
	color: #2d3748;
	text-align: center;
	margin-bottom: 16rpx;
}

/* 页面副标题 */
.page-subtitle {
	font-size: 18rpx;
	color: #718096;
	text-align: center;
}

/* 表单容器 */
.form-container {
	flex-direction: column;
	margin-bottom: 10rpx;
}

/* 表单项 */
.form-item {
	margin-bottom: 10rpx;
}

/* 表单标签 */
.form-label {
	font-size: 16rpx;
	color: #2d3748;
	margin-bottom: 20rpx;
	font-weight: 600;
}

/* 输入框包装器 */
.input-wrapper {
	background: #ffffff;
	border-radius: 16rpx;
	border: 1rpx solid #e2e8f0;
	box-shadow: 0 4rpx 12rpx rgba(0, 0, 0, 0.05);
}

/* 表单输入框 */
.form-input {
	padding: 32rpx 24rpx;
	font-size: 12rpx;
	color: #2d3748;
	height: 100rpx;
	line-height: 100rpx;
}

/* 文本域包装器 */
.textarea-wrapper {
	background: #ffffff;
	border-radius: 16rpx;
	border: 1rpx solid #e2e8f0;
	box-shadow: 0 4rpx 12rpx rgba(0, 0, 0, 0.05);
}

/* 表单文本域 */
.form-textarea {
	padding: 32rpx 24rpx;
	font-size: 12rpx;
	color: #2d3748;
	height: 200rpx;
	line-height: 40rpx;
}

/* 封面区域 */
.cover-section {
	flex-direction: column;
}

/* 封面按钮 */
.cover-button {
	flex-direction: row;
	align-items: center;
	justify-content: center;
	background: #ffffff;
	border-radius: 16rpx;
	padding: 32rpx;
	border: 1rpx solid #e2e8f0;
	box-shadow: 0 4rpx 12rpx rgba(0, 0, 0, 0.05);
	margin-bottom: 20rpx;
}

.cover-button-icon {
	font-size: 32rpx;
	margin-right: 12rpx;
	color: #718096;
}

.cover-button-text {
	font-size: 18rpx;
	color: #2d3748;
	font-weight: 500;
}

/* 封面预览 */
.cover-preview {
	position: relative;
	align-self: center;
}

/* 封面图片 */
.cover-image {
	width: 300rpx;
	height: 300rpx;
	border-radius: 16rpx;
	border: 2rpx solid #e2e8f0;
}

/* 移除封面按钮 */
.cover-remove {
	position: absolute;
	top: -10rpx;
	right: -10rpx;
	width: 40rpx;
	height: 40rpx;
	background: #e53e3e;
	border-radius: 20rpx;
	justify-content: center;
	align-items: center;
	border: 2rpx solid #ffffff;
}

.remove-icon {
	font-size: 20rpx;
	color: #ffffff;
	font-weight: bold;
}

/* 操作区域 */
.action-area {
	align-items: center;
	justify-content: center;
}

/* 开始直播按钮 */
.start-button {
	flex-direction: row;
	align-items: center;
	justify-content: center;
	background: #3182ce;
	border-radius: 50rpx;
	padding: 32rpx 80rpx;
	min-width: 400rpx;
	box-shadow: 0 8rpx 20rpx rgba(49, 130, 206, 0.3);
}

.start-button-text {
	font-size: 36rpx;
	color: #ffffff;
	font-weight: bold;
}

/* #ifdef APP-NVUE */
/* NVUE特定样式 */
.page-container {
	background-color: #f6f8fb;
}

.input-wrapper, .textarea-wrapper, .cover-button {
	background-color: #ffffff;
	border-color: #e2e8f0;
}

.start-button:active {
	opacity: 0.9;
	transform: scale(0.98);
}

.input-wrapper:focus-within, .textarea-wrapper:focus-within {
	border-color: #3182ce;
}
/* #endif */

/* #ifdef APP-PLUS */
/* APP-PLUS平台特定样式 */
.start-button:hover {
	transform: translateY(-2rpx);
	box-shadow: 0 12rpx 24rpx rgba(49, 130, 206, 0.4);
}

.start-button:active {
	transform: translateY(1rpx);
	box-shadow: 0 4rpx 12rpx rgba(49, 130, 206, 0.2);
}
/* #endif */
</style>